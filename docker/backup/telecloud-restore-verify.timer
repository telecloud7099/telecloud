[Unit]
Description=Run TeleCloud restore verification weekly

[Timer]
# Sundays at 04:00 -- one hour after the 03:00 daily backup, so there's
# always a fresh snapshot to verify against.
OnCalendar=Sun *-*-* 04:00:00
Persistent=true

[Install]
WantedBy=timers.target
